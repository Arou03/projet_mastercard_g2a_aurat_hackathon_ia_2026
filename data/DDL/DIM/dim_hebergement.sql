create or replace dynamic table M2_ISD_EQUIPE_5_DB.DIMENSION.DIM_HEBERGEMENT(
	CATEGORIE,
	NIVEAU,
	FAMILLE,
	PARENT,
	DESCRIPTION
) target_lag = '1 day' refresh_mode = AUTO initialize = ON_CREATE warehouse = HACKATHON_WH
 as
SELECT DISTINCT
    CATEGORIE,
    NIVEAU,
    FAMILLE,
    PARENT,
    DESCRIPTION
FROM M2_ISD_EQUIPE_5_DB.CLEANED.FACT_FREQUENTATION;