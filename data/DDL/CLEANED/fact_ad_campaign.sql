create or replace dynamic table M2_ISD_EQUIPE_5_DB.CLEANED.FACT_AD_CAMPAIGN(
	DISPOSITIF,
	TYPE_CAMPAGNE,
	CAMPAGNE,
	CODE_PAYS,
	CANAL,
	BUDGET,
	DATE_DEBUT,
	DATE_FIN,
	IMPRESSIONS,
	CLICS,
	CTR,
	VOLUME_BOOKING,
	PAX,
	NUITEES,
	PMV,
	CA,
	ROI
) target_lag = '1 hour' refresh_mode = AUTO initialize = ON_CREATE warehouse = HACKATHON_WH
 as
SELECT
    DISPOSITIF,
    TYPE_CAMPAGNE,
    CAMPAGNE,
    CODE_PAYS,
    CANAL,
    BUDGET,
    TO_TIMESTAMP(DATE_DEBUT::VARCHAR, 'YYYY-MM-DD') as DATE_DEBUT,
    TO_TIMESTAMP(DATE_FIN::VARCHAR, 'YYYY-MM-DD') as DATE_FIN,
    IMPRESSIONS,
    CLICS,
    CTR,
    VOLUME_BOOKING,
    PAX,
    NUITEES,
    PMV,
    CA,
    ROI
FROM M2_ISD_EQUIPE_5_DB.RAW.CAMPAGNE_PUB;